import { PlaceholderPage } from './PlaceholderPage'

export function LaboratoriesPage() {
  return (
    <PlaceholderPage
      title="Laboratories"
      description="Lab_ID, Lab_Name, Location and Contact_No for every processing facility."
      pending="getLabLocations() and createLabLocation() are implemented and already power the laboratory dropdown on the sample intake screen. What remains is the management table and form."
    />
  )
}
